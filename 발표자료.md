# 작품 설명

국내 주식 매매 판단을 돕는 개인 투자자용 웹 서비스다. 한국투자증권 Open API로 실시간 시세와 일봉 데이터를 받아오고, Volume Profile·Anchored VWAP 같은 거래량 기반 알고리즘으로 지지·저항선을 자동 산출한다. 여기에 LLM이 뉴스·공시를 해석한 방향성 신호를 정량·재무 신호와 합쳐 종합 점수를 만든다.

점수의 근거가 된 뉴스 기사·공시·재무 지표는 `evidence`로 묶여 사용자가 원문까지 추적할 수 있다. 단순히 "매수/매도" 결과만 보여주는 대신 판단 과정을 들여다볼 수 있게 한 점이 핵심이다. 또 거래량 급등·골든크로스·연속 순매수 같은 8종 조건을 조합한 검색식으로, 보유하지 않은 종목 중에서도 매매 기회를 발굴할 수 있다.

# 제작의도

직접 주식 투자를 하며 이익은 성급히 실현하고 손실은 방치하는 처분효과로 큰 손해를 겪었습니다. 감정을 배제한 기계적 매매가 필요했지만, 기존 퀀트 투자는 수치 데이터에만 의존해 뉴스·공시 같은 시장 맥락을 반영하지 못하는 한계가 있었습니다. 이를 해결하고자 비정형 데이터까지 통합 분석하고, 판단 근거를 투명하게 제공하는 서비스를 개발했습니다.

# 주요 기능

- 실시간 시세 제공 (현재가·등락률, 당일 시가/고가/저가, 거래량·거래대금, 52주 최고·최저가)
- 주식 전망 분석 (정량·LLM·재무 신호 합산 점수, 뉴스·공시 근거, 재무 해석의 위험·긍정 요인, 보유 종목 손익 컨텍스트)
- 다중 조건 검색식 (거래량 급등, 골든크로스, 외국인·기관 연속 순매수, 급등주, 체결강도, 신고가 근접, 상한가)

# 시스템 설계

```
┌─────────────────┐
│  Next.js (프론트)│  React 19, TypeScript, Tailwind, lightweight-charts
└────────┬────────┘
         │ REST + WebSocket
┌────────▼────────┐
│  FastAPI (백엔드)│  Python 3.11, Pydantic, asyncio, OpenAI API
└────────┬────────┘
         │
   ┌─────┼──────────┐
   ▼     ▼          ▼
 SQLite  KIS REST   KIS WebSocket
(screener)
```

- **프론트엔드**: Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS, lightweight-charts
- **백엔드**: FastAPI, Python 3.11, Pydantic, asyncio, websockets, OpenAI API
- **데이터베이스**: SQLite (일봉·투자자 데이터 캐시, 스크리너 인덱스)
- **외부 API**: KIS Open API (REST 시세·주문, WebSocket 실시간 체결가)

# 핵심 기술 설명

**Volume Profile + Anchored VWAP (지지/저항 자동 탐지)**

일봉 거래량을 가격 구간(bucket)에 분배해 POC(거래량 최다 가격대)와 HVN(평균+1σ 이상 가격대)을 찾고, 52주 최고가·최저가·거래량 급증일을 앵커로 한 VWAP을 함께 계산해 지지·저항선을 산출한다. 단순 swing point 기반은 노이즈가 많고 주관적인 반면, 거래량 가중 방식은 시장이 실제로 합의한 가격대를 정량적으로 잡아낸다.

**다중 퀀트 신호 합산 (전망 점수 산출)**

서로 다른 관점의 정량 지표 5종을 한 종목에 대해 동시에 계산하고, 합산해 정량 점수를 만든다.

- **골든크로스 (MA5/MA20)** — 단기/장기 이동평균 교차 + 추세 유지 상태
- **이격도 (MA20 대비)** — 현재가가 20일 이동평균에서 떨어진 거리
- **모멘텀 (60일 수익률)** — 60일 누적 수익률 기반 추세 강도
- **거래량 급증** — 당일 거래량 / 20일 평균 비율
- **외국인 수급** — 외국인 순매수 3일 누적량

단일 지표는 노이즈나 가짜 신호에 취약하지만, 추세(이평선)·과열(이격도·모멘텀)·수급(거래량·외국인)을 함께 보면 서로 다른 차원의 신호가 합의할 때만 점수가 강하게 나와 false positive를 줄인다.

**KIS WebSocket Fan-out (asyncio.Queue)**

KIS WebSocket에 단일 연결을 유지하면서 브라우저별로 asyncio.Queue를 만들어 틱을 종목 구독자에게 fan-out한다. 종목별 구독자가 0명이 되면 KIS 구독도 자동 해제한다. 클라이언트마다 KIS 연결을 만들면 동시 접속 한도(40종목)에 금세 도달하기 때문에, 단일 connection + 메모리 큐 구조를 택했다.

**KRX/NXT 시간대 자동 전환**

KST 기준 시간으로 시장 분류 코드를 결정한다. 08:00~09:00, 15:30~20:00은 NXT, 그 외는 KRX. 시세·랭킹·관심종목 시세 API가 동일 함수를 공유해 일관성을 유지한다. 사용자가 시간대를 신경 쓰지 않아도 거래 가능한 시장의 시세를 끊김 없이 받는다.

**LLM 기반 뉴스·공시 해석**

수집된 뉴스·공시 텍스트를 OpenAI API에 전달해 긍정·부정·중립 방향과 요약, 신뢰도를 추출한다. 결과는 `evidence_id`로 점수에 연결돼 사용자가 근거를 역추적할 수 있고, 별도 캐싱 레이어로 동일 입력 중복 호출을 막는다. 정량 데이터만으로는 인수합병·실적 가이던스·규제 이슈 같은 단기 이벤트를 반영하기 어려워, 비정형 텍스트를 정량 점수와 결합하는 방식을 택했다.
