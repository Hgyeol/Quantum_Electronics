# '서버 구축 및 운용' 프로젝트 기획서

## 1. 프로젝트 기본 정보

```
● 프로젝트 명: Quantum Electronics
● 프로젝트 한 줄 요약
퀀트·뉴스·공시·수급 신호를 종합 분석해 한국 주식 매수·매도 시점과
판단 근거를 투명하게 제공하는 AI 기반 투자 전망 서비스
```

## 2. 기획 배경 및 목적

● 아이디어 제안 배경

주식 투자 과정에서 이익은 성급히 실현하고 손실은 방치하는 처분효과로 인해 큰 손실을 경험하며, 감정을 배제한 기계적 매매의 필요성을 절감했습니다. 그러나 기존 퀀트 투자는 정형화된 수치 데이터에 의존하는 한계로 시장의 맥락을 충분히 반영하지 못하고, 매매 판단 근거가 불투명한 블랙박스 구조로 인해 전략을 개선하거나 신뢰하기 어렵다는 문제가 있었습니다. 이에 따라 뉴스, 공시 등 비정형 데이터까지 통합적으로 분석하고, 매매 판단의 근거를 투명하게 제공하는 AI 기반 투자 전망 서비스를 기획하게 되었습니다.

● 핵심 기능

| 구분 | 기능 | 설명 |
|------|------|------|
| SW | 퀀트 신호 분석 | 골든크로스·이격도·모멘텀·외국인 수급·거래량 급증을 결정론적으로 계산해 신호 생성 |
| SW | LLM 텍스트 분석 | 뉴스·공시를 OpenAI GPT로 분석해 투자 관련 근거 및 방향성 추출 |
| SW | ML 예측 | 퀀트·재무·LLM 신호를 학습한 Logistic Regression 모델로 다음날 방향 예측 |
| SW | 재무 지표 분석 | DART 재무제표에서 PER·PBR·ROE 등 핵심 지표를 결정론적으로 산출 |
| SW | XAI 대시보드 | 신호별 기여도를 분해해 어떤 근거로 결론이 도출됐는지 시각화 |

## 3. 기대 효과

감정적 편향 없이 퀀트 신호와 AI 분석에 기반한 객관적인 투자 전망을 제공합니다. 퀀트·재무·LLM·ML 4가지 신호를 종합한 복합 스코어로 한국 주식의 매수·매도 적정 시점을 제시하고, AI가 참고한 뉴스·공시·외국인 수급 데이터를 신호별 기여도와 함께 시각화해 기존 자동매매의 불투명한 블랙박스 구조를 해소합니다.

## 4. 프로그램 설계

● 시스템 구조

```
사용자 ↔ Next.js(프론트) ↔ FastAPI(백엔드) ↔ KIS API / DART API / Naver News API / OpenAI API
```

● 주요 기술 스택

| 분야 | 기술 |
|------|------|
| Backend | Python 3.11 + FastAPI |
| Frontend | Next.js (App Router) + Tailwind CSS |
| AI/LLM | OpenAI GPT (뉴스·공시 텍스트 분석) |
| ML | Scikit-learn Logistic Regression (신호 학습) |
| 증권 API | 한국투자증권(KIS) API (시세·수급·뉴스) |
| 공시 API | DART Open API (재무제표·공시) |
| 뉴스 | Naver 뉴스 검색 API |
| 데이터 저장 | CSV (주가 히스토리), JSON (LLM 캐시, ML 아티팩트) |

● 데이터 저장 계획

| 데이터 | 저장 방식 |
|--------|----------|
| 주가 OHLCV | KIS API → CSV (data/prices.csv) |
| 퀀트·재무·LLM 신호 피처 | CSV (ml/artifacts/signal_learning_v1/) |
| LLM 분석 결과 캐시 | JSON (data/llm_cache.json) |
| ML 모델 아티팩트 | JSON (ml/artifacts/signal_learning_v1/) |

## 5. 예상 최종 결과물

- `GET /outlook/stock/{code}`: 종목 코드 입력 → OutlookReport (퀀트·LLM·ML·재무 신호 + 종합 전망 + 근거 목록)
- Next.js 대시보드: FinalVerdictCard, SignalBreakdownPanel, QuantSignalsTable, LLMSummaryCard, StockPriceChart, EvidenceList

## 6. 개발 일정

| 단계 | 주요 활동 |
|------|----------|
| 환경 구축 | KIS API 연동, 뉴스·지표 수집 파이프라인 구현 |
| 신호 설계 | 퀀트 신호·재무 지표·LLM 분석 모듈 개발 |
| ML 파이프라인 | 신호 학습·백테스트·Logistic Regression 훈련 및 FastAPI 통합 |
| 통합 및 시각화 | FastAPI 엔드포인트, Next.js 프론트 연동, XAI 신호 시각화 |
